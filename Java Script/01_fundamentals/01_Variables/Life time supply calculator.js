const currectAge = 23;
let maxAge = 200;
let amoutPerDay = 3;
	let rest = (maxAge - currectAge)*(amoutPerDay*365);
		console.log(`you will need ${rest} to last you until the ripe old age of ${maxAge - currectAge}`);
